<h1>Cadastrar novo Pedido:</h1>
<p>Preencha primeiro as informações do cliente:</p>

<%= form_with(model: [@restaurant, @order] ) do |f|%>
  <div>
    <%== f.label :client_name, 'Nome do cliente' %>
    <%= f.text_field :client_name, required: true%>

    <%== f.label :cpf, 'CPF' %>
    <%= f.text_field :cpf, required: false%>

    <%== f.label :phone, 'Telefone' %>
    <%= f.text_field :phone, required: false%>

    <%== f.label :email, 'E-mail' %>
    <%= f.text_field :email, required: false%>
    <%= f.submit 'Iniciar Pedido'%>
  </div>
<% end %>