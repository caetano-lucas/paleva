<h1>Pré cadastre um novo funcionário:</h1>

<%= form_with(model:[@restaurant, @employee]) do |f| %>
  <div>
    <%= f.label :email, 'E-mail'%>
    <%= f.text_field :email %>
  </div>
  <div>
    <%= f.label :cpf, 'CPF'%>
    <%= f.text_field :cpf %>
  </div>
  <%= f.submit 'Salvar Funcionário' %>
<% end %>