  <p><%= link_to 'Características adicionais', restaurant_features_path %></p>

<h1>Detalhes do prato</h1>
<p><%= link_to 'Adicionar característica', new_restaurant_dish_dish_feature_path(dish_id: @dish.id) %></p>

<table>
   <thead>
   <tr>
       <th>Foto </th>
       <th>Nome do Prato</th>
       <th>Descrição</th>
       <th>Calorias</th>
       <th>Contém</th>
       <th>Status</th>
       <th>Ações</th>
   </tr>
   </thead>
   <tbody>
      <tr>
        <td>
          <% if @dish.image.attached? %>
            <%= image_tag @dish.image, size: "100x100", alt: @dish.name %>
          <% else %>
            <p>Sem imagem disponível</p>
          <% end %>
        </td>
        <td> <%= @dish.name %> </td>
        <td> <%= @dish.description %> </td>
        <td> <%= @dish.calories %> </td>
        <td> 
          <% @dish.dish_features.each do |dish_feature| %>
            <%= dish_feature.feature.name %>
          <% end %>
        </td>
        <td> <%= I18n.translate(@dish.status) %> </td>
        <td> <%= button_to 'Alterar Status', change_status_restaurant_dish_path(@restaurant, @dish) %></td>
        <td> <%= link_to "Editar #{@dish.name}", edit_restaurant_dish_path(current_user.restaurant, @dish) %></td>
      </tr>
   </tbody>
</table>

