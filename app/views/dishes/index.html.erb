<h1>Lista de Pratos</h1>
<p><%= link_to 'Cadastrar novo prato', new_restaurant_dish_path%></p>
<table>
   <thead>
   <tr>
       <th>Nome do Prato</th>
       <th>Descrição</th>
       <th>Calorias</th>
       <th> Foto </th>
       <th>Ações</th>
   </tr>
   </thead>
   <tbody>
    <% @dishes.each do |d| %>
      <tr>
        <td> <%= d.name %> </td>
        <td> <%= d.description %> </td>
        <td> <%= d.calories %> </td>
        <td>
          <% if d.image.attached? %>
            <%= image_tag d.image, size: "100x100", alt: d.name %>
          <% else %>
            <p>Sem imagem disponível</p>
          <% end %>
        </td>
        <td> <%= link_to "Editar #{d.name}", edit_restaurant_dish_path(current_user.restaurant, d) %> </td>
        <td> <%= button_to "Deletar #{d.name}", restaurant_dish_path(current_user.restaurant, d), method: :delete %> </td>
      </tr>
   <% end %>
   </tbody>
</table>